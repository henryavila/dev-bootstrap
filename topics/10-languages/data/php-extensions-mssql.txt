# Microsoft SQL Server PHP extensions (opt-in).
# Enabled by INCLUDE_MSSQL=1 or the corresponding menu checkbox.
#
# Prerequisites (installed automatically by scripts/install-mssql-driver.sh):
#   - Microsoft APT repo + GPG keyring (Linux)
#   - msodbcsql18 (ODBC driver, required at runtime)
#   - mssql-tools18 (sqlcmd, bcp — convenience CLI tools)
#   - unixodbc-dev (headers required to compile the PECL extensions)
#   - ACCEPT_EULA=Y (Microsoft licence terms — prompted/logged)
#
# Connection string note: msodbcsql18 enforces TLS 1.2+. Self-signed server
# certs (common in corporate deployments) require:
#   Encrypt=yes;TrustServerCertificate=yes
#
# Format: <extension>

sqlsrv
pdo_sqlsrv
