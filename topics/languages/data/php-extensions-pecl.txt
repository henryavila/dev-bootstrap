# PHP extensions installed via PECL (not in apt/brew as separate packages).
# PECL policy:
#   - Every non-comment entry in this file is part of the mandatory baseline.
#   - install()/repair() must attempt each extension for every PHP_VERSIONS entry.
#   - A PECL build failure, missing .so, or active .ini that points at a missing
#     .so is a broken PHP stack state, not a successful install.
#   - verify() must fail until each baseline extension either loads cleanly for
#     each declared PHP version or is covered by a future explicit quarantine
#     decision. Do not silently skip baseline entries.
#
# Build dependencies are declared inline after `:` (apt packages on Linux,
# brew formulas on Mac). Empty/missing means no extra deps needed.
#
# Format: <extension>[:<linux-deps>[:<mac-deps>]]

igbinary
imagick:libmagickwand-dev pkg-config:imagemagick pkg-config
mongodb:libssl-dev:openssl
pcov
redis
