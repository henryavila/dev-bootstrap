# ~/.zshrc.local — loaded LAST by ~/.zshrc generated by mesh-workstation.
#
# This is where your personal zsh setup goes:
# - zinit plugins (turbo-loaded)
# - Powerlevel10k instant prompt
# - Custom completions registration
# - fpath additions
#
# The template ships empty. Build it up as you configure your shell.
