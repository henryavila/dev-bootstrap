# ~/.bashrc.local — loaded LAST by ~/.bashrc (generated by mesh-workstation/30-shell).
# Personal aliases live in ~/.bashrc.d/99-personal-aliases.sh (deployed from shell/aliases.sh);
# this file is for shell options, PATH tweaks, and interactive-only setup.

# Example: ble.sh (fish-like autosuggestions for bash)
# [[ -f "$HOME/.local/share/blesh/ble.sh" ]] && source "$HOME/.local/share/blesh/ble.sh" --noattach

# Example: extra shell options
# shopt -s globstar 2>/dev/null || true
